usecontetx is a react hook that allows u access data from any component without explicitily 
passing it down through props at every level.

OR

useContext is used to manage global data in rect app.
it happens in 3 steps :
1 -> create a context
2- >Provide the context
3 -> use the context in the component where you want to access the data.(cosume the context)